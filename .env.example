# Chú thích: Environment variables cho STEM Vietnam v2
# Copy file này thành .env.local và điền giá trị phù hợp

# API URL cho backend (Cloudflare Workers)
# Development: http://localhost:8787
# Production: https://stem-vietnam-api.<your-subdomain>.workers.dev
VITE_API_URL=http://localhost:8787

# Chú thích: VITE_GEMINI_API_KEY không cần nữa vì backend xử lý
# API key được lưu an toàn trong Cloudflare Workers secrets

